# Deprecated: Centrifugo is replaced by WuKongIM.
# See nginx-api-wukongim.example.conf (location /wkws/ → :5200).

#
# Clients connect to:
#   wss://api.example.com/connection/websocket
#   https://api.example.com/connection/http_stream
# instead of a separate ws.example.com hostname (DNS/CDN/Upgrade failures).
#
# Keep ws.example.com as an optional alias that proxies to the same upstream.
#
# Requires:
#   map $http_upgrade $connection_upgrade {
#       default upgrade;
#       ''      close;
#   }

# server {
#     listen 443 ssl http2;
#     server_name api.example.com;
#
#     location /api/ {
#         proxy_pass http://127.0.0.1:9000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /connection/ {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection $connection_upgrade;
#         proxy_read_timeout 3600s;
#         proxy_send_timeout 3600s;
#         proxy_buffering off;
#     }
# }
#
# # Optional alias for old clients:
# # server {
# #     listen 443 ssl http2;
# #     server_name ws.example.com;
# #     location / {
# #         proxy_pass http://127.0.0.1:8000;
# #         proxy_http_version 1.1;
# #         proxy_set_header Upgrade $http_upgrade;
# #         proxy_set_header Connection $connection_upgrade;
# #         proxy_read_timeout 3600s;
# #         proxy_send_timeout 3600s;
# #         proxy_buffering off;
# #     }
# # }
