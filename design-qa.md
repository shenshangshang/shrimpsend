# 全页面 redesign-v3 设计验收

日期：2026-09-21。已完成本轮范围的页面实现、本地部署与回归。

final result: passed（本地 Web / macOS 范围；外部服务与移动真机边界见末节）

## 设计依据

用户批准的基准为 `design/redesign-v3/manifest.json`，17 张画板共 69 个页面/状态画面。会员、账号、反馈使用清单中的 v2。统一规范以同目录 README 为准；旧「方案 2」报告已归档到 `docs/testing/2026-09-21-device-conversation-design-qa.md`。

## 对照与证据

- 同一图像输入对照：00 主会话与实际 Web 产品截图；06 设置与真实 Mac 通用页；08 WebDAV 与真实 Mac 云端列表。官网、文档、法律模板及手机页面另通过浏览器检查。
- Web 桌面实际产品图：`web/public/product-preview.png`，1280 × 720；来自真实测试设备的成功文字与文件发送记录。
- 浏览器补充截图：`.local/redesign-qa/web-*.png`，包含官网、文档、账号、手机设置/授权/深色/任务/法律。
- 原生运行截图：`.local/native-ui-qa/01-transfer.png` 至 `22-cloud-files.png`。真实字体、图标与窗口，非测试字体替代。因含用户本机文件与目录，不放入公共素材。
- 测试生成的 `.local/redesign-qa/flutter-*.png` 只辅助布局检查，不用于判断真实字体和图标。

## 视觉与交互复核

| 项目 | 实现与检查 |
| --- | --- |
| 层级 | 三个一级入口、五组设置、六组文件；云端内部筛选使用页内标签，去除第二组底部主导航 |
| 密度 | 桌面 72px 主栏、220–264px 次栏；14–16px 正文，8px 按钮、12px 容器；会话限制宽度，保持主要操作集中 |
| 颜色 | 批准的近白/深绿与深色主题；统一边框、表面色，深色选中标签使用柔和强调底色 |
| 小屏 | 390px 浏览器实查及 Flutter 39 组布局断言；修复任务列表和名称输入横向溢出 |
| 品牌 | 虾形标志统一用于应用导航、官网和桌面图标；线性功能图标保持一致 |
| 状态 | 真实文件进度、空/加载/失败、授权等待、名额已满、目录权限与连接诊断，不展示虚构速度或假订单 |
| 交互 | 跨页保持 WebRTC、草稿、实际 WebDAV 文件操作、接收目录设置、主题/语言与账号入口 |

## 本轮发现并处理

- Web 手机任务列过宽、设备名输入超出窄屏：收敛列布局和输入宽度。
- Mac 云端页遗留旧底部导航：改为页内标签与上传按钮。
- Mac 深色选中标签不清晰：改用强调浅底色。
- 官网预览曾截到布局切换中间态：以稳定桌面视图重新截取并复核。
- 更新检查失败占据全局错误条：改为手动检查时反馈，实际安装错误仍显示。
- Web 服务重启触发开发错误浮层：处理背景查询失败并在联网后重试。
- 原生连接保存和 WebDAV 正常 204 响应失败：修复钥匙串兼容配置及成功状态判断，并增加实际 HTTP 回归。

最终原生集成 1/1 通过：20 个目的页面和连接表单/云端列表共 22 张真实截图；WebDAV 上传下载字节一致。正常 Mac 构建和 Docker 部署完成。Web 生产版验证游客方案、订单登录提示与后台登录门禁。

## 合理差异与验证范围

实际文件、设备、账号、套餐数量与截图示例不同。浏览器接收目录、打开文件、系统权限和平台专有功能按真实能力呈现；没有为了对齐图片加入无效控件。法律文本保持现有内容，仅统一阅读模板。管理后台未执行实际发布、支付未发生，移动布局不等于移动真机测试。详细结果见 `docs/testing/2026-09-21-redesign-implementation.md`。

## 后续用户反馈：Flutter 切页带动侧栏

初次实现中的每页独立导航确实导致整页过渡。现已用常驻工作区替换，文件/设置二级栏位于内容 Navigator 外，主入口不再压入整页路由。4 项导航回归、349 项 Flutter 回归（1 项原有跳过）及真实 Mac 22 个页面/状态检查通过；逐页验证主导航 Element 和坐标不变，ChatScreen State 不重建。详见 `docs/testing/2026-09-21-flutter-navigation.md`。
